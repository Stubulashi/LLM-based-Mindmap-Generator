# AI 的使用——AI 辅助工具与工作流

## 概述

本文档按少数几个工作块披露在项目落地过程中 AI 辅助工具的使用情况，而非以穷举式条目清单呈现。文档开篇即明确基本立场：AI 工具在此过程中主要承担「支持」角色。它们用于审阅、检索、批评与建议，而设计决策、实现选择、代码本身及其验证，仍由开发者本人主导负责。这类技术是被当作能够加速并精炼工作的伙伴来使用，而非当作可以独立撰写成果的写作者。

## 使用的工具

项目中使用的主要大语言模型工具包括：DeepSeek（v4-flash 与 v4-pro）、DeepL、Qoder、Gemini 与 GPT。DeepL 用于语言层面的核验，其余工具则在下文所述的不同工作块中服务于不同目的。DeepSeek 的两种变体是日常编程辅助的主力；Qoder 是承载其中大多辅助的集成开发环境；Gemini 与 GPT 用于规划阶段的广度探索与审阅。凡对变体（flash 或 pro）做出选择处，均反映所面对任务类型的不同：较轻量的变体用于常规交互，较重的变体用于要求更高的分析。

## 规划与可行性工作

在早期阶段、任何代码落笔之前，与 LLM 就项目 proposal 的若干细节进行了充分讨论，涵盖更具体的可行性评估、预计会遇到的难点，并在若干处涉及工作顺序的安排。与此同时，采用了反向角色的思考方式：要求模型扮演外部评审者，就具体要点提出细致的问题，以便对所得理解进行正确性与合理性的核验，而非径信表面判断。提示词也经过刻意措辞，避免在代码中生成具体案例，从而防止现成样例把分析引向固定思路，即所谓「思维定势」问题。同一 LLM 还协助收窄了值得探究的范围：它帮助细化候选模型、理清可行的开发方向并权衡所涉工作量，又帮助评估并舍弃若干被认为无意义或过于发散的功能与实现方法。最后，也请 LLM 在网络中检索是否存在类似的开源项目或 Git 仓库，使既有工作能够作为参考并从中借鉴可复用的思路，而非一切从零重新发明。

## 编码辅助

DeepSeek 是实现阶段编程辅助中使用的主要 LLM。其中 v4-flash 变体通过 Qoder IDE 接入，在编码过程中提供实时的代码审阅、改错及下一步建议，从而缩短了「编写—修改」代码的反馈闭环。在此期间，DeepSeek 也推出了 harness 功能并对其进行了尝试，其产出的结果被用于对代码进行复核；然而出于个人原因，该功能对本项目的帮助较为有限，因此并未被采纳为主线工作流步骤。

由于集成开发环境可以总览工作区内的全部文件，因此使用了 IDE 的侧边栏（Quest）模式进行跨文件分析：追踪仓库内函数之间的嵌套关系，以及变量的引用链条。在同一模式下，也用于在项目期间生成并归档每日或每阶段的工作日志。

就运行环境而言，学校集群当时仍较为陌生、仍处于学习阶段，尚未成为可靠的模型调用通道。因此，所有需要调用 LLM 的部分（包括由 Qoder IDE 环境驱动或由 agent 驱动）均来自 DeepSeek API。不同变体（flash 与 pro）根据每次调用的用途与作用范围来选择。

## 前端与渲染集成

组内对前端开发的经验较为有限，且尚未掌握可靠的前后端打通（FastAPI 体系）能力。因此在前端这一部分工作中，LLM 的使用率相对较高。这一点在后期前端导图渲染迁移至组内较为陌生的 AntV G6 系统时表现得尤为明显。将该系统集成进项目，涉及理解一种陌生的渲染模型、调整数据流，并对由此产生的交互进行调试，LLM 在上述每一步中都发挥了较大作用。此处估算其贡献约为 70%。需要强调的是，该数字为一近似值，且主要反映渲染集成这一环节；它并非对项目整体贡献比例的表述。

## 结语

总体而言，AI 工具的使用集中于三个方向：早期规划与范围界定、IDE 内的代码审阅与辅助，以及陌生渲染库的集成。在每一种情形下，产出物在被采纳之前，均经过开发者本人的审阅、调整与验证。这些工具加速了工作可被探索的速度，但「接受什么、舍弃什么、改变什么」的取舍，自始至终都保留在人的判断之中。
