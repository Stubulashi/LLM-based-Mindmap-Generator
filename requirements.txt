annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.4.22
click==8.3.3
distro==1.9.0
fastapi==0.136.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.13
jiter==0.14.0
openai==2.33.0
pydantic==2.13.3
pydantic_core==2.46.3
python-dotenv==1.2.2
sniffio==1.3.1
starlette==1.0.0
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.46.0
mcp>=1.0.0
wikipedia-api>=0.7.0
openai-whisper
sentence-transformers>=2.2.0

# C: 可选依赖 — 离线拼音生成（未安装时自动回退 LLM）
# E: Optional — offline pinyin generation (falls back to LLM when not installed)
# pypinyin>=0.49.0

# C: 评估框架依赖（缺失时自动降级/提示，具体依赖树见 evaluation/run_evaluation.METHOD_DEPENDENCIES）
# E: Evaluation framework dependencies (auto-degrade / prompt when missing; full tree in evaluation/run_evaluation.METHOD_DEPENDENCIES)
# C: numpy 为评估必需（aligner / embedder / 层级与标签指标），此前缺失已补齐
# E: numpy required by evaluation (aligner / embedder / hierarchy & label metrics), now added back
numpy>=1.26.0
jiwer>=3.0.0
jieba>=0.42.1
# C: zss（nTED）与 scipy（匈牙利指派/统计）为 §2 层级指标必需，声明以免全新安装时 Required 指标静默缺失
# E: zss (nTED) and scipy (Hungarian assignment/statistics) are required by §2 hierarchy metrics
zss>=1.2.0
scipy>=1.10.0
# C: 评估的 QA 维度已重构为仅依赖 OpenAI，以下旧语言处理依赖已不在主评估管线使用：
#    nltk / rouge-score / bert-score 已移除。rouge-score 仅 test/paper 实验脚本使用，按需自行安装：pip install rouge-score
# E: The QA evaluation dimension now depends only on OpenAI; the legacy NLP libs below are no longer used by the main pipeline:
#    nltk / rouge-score / bert-score removed. rouge-score is only used by test/paper experiment scripts, install on demand: pip install rouge-score
