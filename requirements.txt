annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.4.22
click==8.3.3
distro==1.9.0
fastapi==0.136.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.13
jiter==0.14.0
openai==2.33.0
pydantic==2.13.3
pydantic_core==2.46.3
python-dotenv==1.2.2
sniffio==1.3.1
starlette==1.0.0
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.46.0
mcp>=1.0.0
wikipedia-api>=0.7.0
openai-whisper
sentence-transformers>=2.2.0

# C: 可选依赖 — 离线拼音生成（未安装时自动回退 LLM）
# E: Optional — offline pinyin generation (falls back to LLM when not installed)
# pypinyin>=0.49.0

# C: 评估框架依赖（可选，缺失时自动降级）
# E: Evaluation framework dependencies (optional, auto-degrade when missing)
nltk>=3.8.0
rouge-score>=0.1.2
bert-score>=0.3.13
jiwer>=3.0.0
jieba>=0.42.1
# C: zss（nTED）与 scipy（匈牙利指派/统计）为 §2 层级指标必需，声明以免全新安装时 Required 指标静默缺失
# E: zss (nTED) and scipy (Hungarian assignment/statistics) are required by §2 hierarchy metrics
zss>=1.2.0
scipy>=1.10.0
