# =====================================================================
# AI MindMap Agent - .gitignore
# 约定：本仓库仅提交源代码、文档、金标准/音频等参考输入数据；
#       运行生成的「评估报告 / 会话数据 / 调试输出」等一律不纳入版本控制。
# =====================================================================

# ---------------------------------------------------------------------
# 1. 密钥与配置文件（严禁上传，含 API Key）
# ---------------------------------------------------------------------
.env
*.env
.env.local
.env.*.local
api.env

# ---------------------------------------------------------------------
# 2. Python 缓存 / 虚拟环境
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
*.so
.Python
venv/
env/

# ---------------------------------------------------------------------
# 3. IDE / 编辑器
# ---------------------------------------------------------------------
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
.idea/
*.swp

# ---------------------------------------------------------------------
# 4. HuggingFace 模型缓存
# ---------------------------------------------------------------------
.hf_cache/

# ---------------------------------------------------------------------
# 5. 调试输出（业务层三层分离中的 debug_output）
# ---------------------------------------------------------------------
debug_output/

# ---------------------------------------------------------------------
# 6. 评估报告（根目录 / evaluation 下生成的 Markdown 报告）
# ---------------------------------------------------------------------
# 各批次在线评估的 per-pair 报告
/evaluation/eval_report_*.md
# 示例报告（示例演示模式导出）
/evaluation/eval_report_example_*.md
# 历史报告归档目录（新报告仍生成在 evaluation/ 根目录，保留上述规则）
/evaluation/reports/
# 根目录审计 / 分析报告（由 AI 生成的评估结果，非源码）
evaluation_audit_report.md
hungarian_label_evaluation.md
Report_*.md
eval_report_example_*.md
# ---------------------------------------------------------------------
# 7. 评估会话数据（每次批量评估产生的时间戳目录）
# ---------------------------------------------------------------------
evaluation/data/sessions/

# ---------------------------------------------------------------------
# 8. 其他由管线/评估产生的生命周期结果（非参考输入）
# ---------------------------------------------------------------------
# 最近生成的参考素材（原文-参考树）
reference_example/
# maps 目录：由管线/手工产生的思维导图 JSON 结果
maps/

# ---------------------------------------------------------------------
# 9. Qoder 计划备份等
# ---------------------------------------------------------------------
.qoder/
